@model Northwind.Entities.Cart

<div id="cart">
    <span class="caption">
        <b>
            Your Cart(@Model.Lines.Sum(x=>x.Quantity))
        </b>
        @Model.Total.ToString("c");
    </span>
    @Html.ActionLink("Checkout","Index","Cart")
</div>